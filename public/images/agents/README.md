# Agent Avatars

Drop real avatar PNGs here using these exact filenames. They are already wired
into `src/data/agents.ts` and will replace the generated placeholder avatars
automatically (the UI falls back to a gradient + icon placeholder when a file
is missing).

- architect-agent.png
- coder-agent.png
- reviewer-agent.png
- researcher-agent.png
- tester-agent.png
- devops-agent.png
- manager-agent.png
- analyst-agent.png

Recommended: square images, 256×256 or larger.
